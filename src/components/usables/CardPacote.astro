---
const { titulo, publicoAlvo, preco, beneficios = [] } = Astro.props;
---
<div class="bg-white rounded-xl shadow p-6 flex flex-col">
  <h3 class="text-xl font-title text-primary mb-2">{titulo}</h3>
  <p class="font-body text-sm text-gray-600 mb-4">{publicoAlvo}</p>
  <ul class="text-left text-sm flex-grow space-y-1">
    {beneficios.map((b) => <li>✅ {b}</li>)}
  </ul>
  <p class="mt-4 font-bold text-lg">{preco}</p>
</div>
